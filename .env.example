HOST=0.0.0.0
PORT=1337
APP_KEYS="toBeModified1,toBeModified2"
API_TOKEN_SALT=tobemodified
ADMIN_JWT_SECRET=tobemodified
TRANSFER_TOKEN_SALT=tobemodified
JWT_SECRET=tobemodified

# --- LunaCycle Community ---
# Erlaubte Frontend-Origins für CORS (Komma-getrennt). Dev + LunaCycle-Domain(s).
CORS_ORIGINS=http://localhost:5173,http://localhost:1337
# Base44-"me"-Endpoint zur Token-Verifikation der Auth-Brücke (/api/auth/base44).
# MUSS vor Go-Live mit dem echten Base44-Endpoint gesetzt werden (noch zu bestätigen).
# Beispiel: https://luna-cycle-copy-0dc4c4ae.base44.app/api/auth/me
BASE44_ME_URL=
